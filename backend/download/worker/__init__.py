"""Coordinator worker adapter for the Download service.

This package owns only coordinator transport and action adaptation. Provider
resolution remains in ``archive`` and the existing HTTP API remains separate.
"""
